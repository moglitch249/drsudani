# 🚀 Dr. Sudani Application - System Architecture & Documentation

## 📚 نظرة عامة على النظام (System Overview)
نظام دكتور سوداني (Dr. Sudani) هو تطبيق موبايل متكامل (Flutter) مخصص لبيع المنتجات الرقمية وشحن الألعاب. يعتمد النظام في بنيته التحتية على منصة ووردبريس (WordPress) وإضافة (WooCommerce) لإدارة المنتجات، الطلبات، والعملاء. 

تم تصميم النظام ليكون آمناً جداً ومغلقاً، حيث يتواصل التطبيق مع السيرفر من خلال **طبقة حماية وسيطة (Middleware)** مخصصة لضمان عدم اختراق المحفظة أو التلاعب بالأسعار.

---

## 🏗 البنية المعمارية للنظام (Architecture)

النظام يتكون من 3 أجزاء رئيسية:
1. **تطبيق الموبايل (Flutter App):**
   - واجهة المستخدم وتجربة المستخدم (UI/UX).
   - إدارة حالة التطبيق باستخدام `BLoC` و `Cubit`.
   - إدارة الجلسات (Sessions) وتخزين الـ JWT محلياً باستخدام `Hive`.
   - إضافة طبقة الحماية الأمنية (App Secret + HMAC Signature) في كل طلب (Request).

2. **الخادم الأساسي (WordPress & WooCommerce):**
   - قاعدة البيانات الأساسية للمنتجات، المحفظة (TeraWallet)، والطلبات.
   - إضافة `Simple JWT Login` لإدارة تسجيل الدخول الآمن وإصدار الـ Tokens.

3. **طبقة الحماية الوسيطة (PHP Proxy Middleware - drsudani-wallet-api.php):**
   - هذا هو "حارس البوابة" للسيرفر.
   - يمنع أي تطبيق أو شخص من التخاطب المباشر مع واجهات WooCommerce للطلبات والمحفظة.
   - يحلل الـ JWT Token، يستخرج منه معرّف المستخدم (`User ID`) بأمان، ويقوم بالاستعلام نيابة عنه.
   - يمنع تماماً محاولات "السرقة" أو "تعديل رصيد شخص آخر".

---

## 🔒 نظام الحماية (Security System)

تم بناء 4 طبقات حماية (Layers) للتطبيق:
1. **App Secret Layer:**
   - التطبيق يرسل `X-App-Secret` مشفر في كل طلب.
   - إذا لم يتطابق مع ما هو موجود في `wp-config.php`، يرفض السيرفر الطلب فوراً (Unauthorized application).

2. **HMAC Signature Layer:**
   - التوقيع الرقمي للبيانات (Payload) والوقت (Timestamp) لتجنب هجمات الـ Replay Attack وتعديل البيانات (مثل تغيير السعر أثناء إرسال الطلب).

3. **JWT Validation Layer:**
   - حماية تسجيل الدخول ومصادقة المستخدم. لا يتم الاعتماد على أي معرّف (`Customer ID`) يرسله التطبيق، بل يتم استخراج المعرّف الحقيقي من الـ JWT المشفّر من طرف السيرفر فقط.

4. **Biometric Security Layer:**
   - لا يمكن إتمام عملية الخصم من المحفظة أو الدفع إلا بعد تأكيد الهوية بواسطة البصمة (Fingerprint) أو بصمة الوجه (FaceID) الخاصة بالمستخدم.

5. **Anti-Race Condition Layer (Idempotency & Locks):**
   - التطبيق يستخدم `UUID v4` لمنع تكرار طلبات الدفع (Idempotency).
   - خادم المحفظة (Wallet API) يعتمد على أقفال ذرية بقواعد البيانات `FOR UPDATE` و `INSERT IGNORE` لمنع ثغرات الخصم المزدوج نهائياً.
   - تم تقييد المنتجات الرقمية بحد أقصى (Max Quantity) يبلغ 5 منتجات للطلب الواحد.

---

## ⚙️ الخطوات التشغيلية وطرق الربط (Operational Steps & Integration)

لربط التطبيق بالسيرفر وإعداده للإطلاق، اتبع الخطوات التالية:

### الخطوة 1: إعدادات السيرفر (cPanel)
يجب إضافة هذه الثوابت في ملف `wp-config.php` في الاستضافة لربط التطبيق بقاعدة البيانات والمحفظة:
```php
define('SIMPLE_JWT_PRIVATE_KEY', 'DrSudani_Secret_2026_!@#\$\$');
define('JWT_AUTH_CORS_ENABLE', true);
define('DS_WC_KEY', 'ck_7186ded696c268afe73fd7d1a894615deff798b1');
define('DS_WC_SECRET', 'cs_5b1506517b703c77d50540bbb0478c0a9c58633a');
define('DS_APP_SECRET', 'DrSudaniAppSec2026x9k2P');
define('DS_JWT_SECRET', 'DrSudaniJwtSec2026v1xZ9wQ');
```

### الخطوة 2: إضافة الـ Middleware
- تأكد من رفع الملف الخاص بالحماية والمحفظة وهو `drsudani-wallet-api.php`.
- يجب وضعه في مجلد `wp-content/mu-plugins/` ليعمل كـ (Must-Use Plugin) أو داخل مجلد الإضافات العادية وتفعيله.

### الخطوة 3: إعدادات إضافة Simple JWT Login
- في إعدادات الإضافة على ووردبريس، اذهب إلى تبويب **Decryption**.
- تأكد من **إلغاء تفعيل (Uncheck)** خيار "Base64 encoded".
- تأكد من استخدام المفتاح السري الذي تم تعريفه في `wp-config.php`.

### الخطوة 4: مسارات الـ API (Endpoints)
تم تصميم وإنشاء المسارات الآتية داخل إضافة `drsudani-wallet-api.php` المخصصة لتكون بديلة عن مسارات WooCommerce التقليدية التي تسبب أخطاء (401):
- **رصيد المحفظة:** `GET /wp-json/drsudani/v1/wallet` (يستخرج الرصيد الآمن للمستخدم).
- **إنشاء طلب (خصم من المحفظة):** `POST /wp-json/drsudani/v1/checkout` (يعالج السلة ويخصم بأمان تام).
- **طلباتي (تتبع الطلبات):** `GET /wp-json/drsudani/v1/orders` (يتجاوز تعقيدات حماية WooCommerce ويعرض طلبات المستخدم الحالي فقط بناءً على الـ JWT).

---

## 🚀 تشغيل وبناء التطبيق النهائي
לבناء النسخة النهائية الجاهزة للرفع على المتاجر (Google Play) بأعلى درجات الحماية وتعتيم الكود (Obfuscation)، قم بتشغيل السكريبت التالي:
```bash
build_secure_apk.bat
```
سيقوم هذا السكريبت بـ:
1. جلب التحديثات والمكاتب `flutter pub get`.
2. حجب الكود المصدري لمنع الهندسة العكسية `--obfuscate`.
3. إصدار نسخة جاهزة بامتداد `app-release.apk`.

> **ملاحظة هامة:** تم إلغاء حماية تصوير الشاشة (FLAG_SECURE) لتمكين المستخدمين من التقاط الشاشة ومشاركة التطبيق براحة.

---
**تم تطوير وتوثيق هذا النظام بواسطة مهندس الذكاء الاصطناعي (Antigravity).**
